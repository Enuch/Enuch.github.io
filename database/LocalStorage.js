class LocalStorage {

    static setTask(data) {
        localStorage.setItem();
    }

    static getTask(data) {
        return localStorage.getItem();
    }

    static getLength() {
        return localStorage.length;
    }
}